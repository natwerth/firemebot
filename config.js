// Configure your Worker endpoint here or via the UI button.
// Example: https://firemebot-worker.nat-1fa.workers.dev/analyze
window.FireMeBot = {
  WORKER_URL: "https://firemebot-worker.nat-1fa.workers.dev/analyze" // leave empty and set via the 'Set endpoint' button if deploying on GitHub Pages
};
